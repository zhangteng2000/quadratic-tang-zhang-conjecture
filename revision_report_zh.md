# 三轮内部审查与修改说明

**稿件：** *The quadratic Tang–Zhang inequality for critical points of polynomials*  
**作者栏：** Teng Zhang，按用户提供的信息保留。  
**日期：** 2026 年 9 月 15 日。  
**性质：** 对所提供候选证明的数学、实现、文献和写作审查。本报告不是 Annals of Mathematics 的真实审稿意见，不代表独立同行审稿或录用判断。

## 1. 本次交付与结论范围

交付的英文稿为完整的 17 页 `amsart` 论文，附完整计算证书、原核验器、另行实现的第二核验器、解析常数核查程序和实际运行输出。

论文的核心结论是：对单位圆盘内零点的 n 次多项式，在每个指定零点 ζ 处，有

\[
\sum_{j=1}^{n-1}|\zeta-c_j|^{-2}\ge n-1.
\]

本轮把证明推进到等号分类：等号成立，当且仅当多项式为 A(z^n−ω)，其中 A≠0、|ω|=1。对 λ≥2，幂平均给出同一不等式及同一等号分类。指定零点在开单位圆盘内时，不等式严格。

这不是 λ=1 的证明，也不应将它称为完整解决了 Tang–Zhang 原猜想的全部指数范围。它同样没有证明 Miller 所提出的关于指定零点位置的统一二次距离改进。新稿明确区分了这两个不同的“quadratic”。

本次检索未发现已经覆盖上述全次数二次平均不等式的来源。这里的表述只说明本次检索的结果，不是穷尽所有文献后的优先权认证。文献中的普通 Sendov 定理不能直接推出这个平均不等式。

## 2. 第一轮：数学归约与原证书

重新检查了归一化、两条原点恒等式、极向积分、均值和方差估计、有限参数盒的推导以及大次数尾部。原证书和解析常数核查程序均重新运行通过。重要处理如下。

### 2.1 不偷换平均假设

普通 Sendov 的倒数坐标反例假设是逐点 |q_j|<1；本稿只有平均平方不超过一。所有删去一个因子的乘积界，均从平均平方和出发。没有将普通 Sendov 证明中的逐点假设偷偷移入这里。

### 2.2 中心化分母与非零条件

明确写出 a<1、|μ|≤1，故 |atμ|<1，中心化分母 D(t)=1−atμ 不会为零。μ≠0 则由极向估计 x>a/2 得到，不是在需要除法时直接假定。

### 2.3 极向参数的单调性

参数盒的第二极向检验通过一个函数 Φ 的上界来实现。但正确的单调对象是弦积分，而不需要声称 Φ 本身单调。新稿 Lemma 6.1 直接将 Φ(h₀)<0 转成弦积分小于一，再与实际参数的弦积分比较，消除了这一处可能被误读的推理。

### 2.4 辅助二次式的截零：复核后不认定为实质漏洞

初审将程序中的 `max(0,...)` 列为待核点。进一步检查表明，合法参数下，原辅助函数已经满足

\[
b(t)=1-h_0t-a_-^2t(1-t)
=(1-t)^2+(1-h_0)t+(1-a_-^2)t(1-t)>0.
\]

因此它不是一个导致原证书失效的负值问题。最终稿使用原二次式，不再引入多余的正部函数；附录解释，程序对定向舍入区间进行非负截断，不会丢弃其真实值。这里补充的是论证透明度，并简化了最初的修补方案。

### 2.5 连续参数覆盖，而非采样

证明要求覆盖整个连续 a 区间。两份程序检查每个整数次数块内的 a 区间精确首尾相接，同时检查次数块之间无缺口。所有排除不等式都有严格余量；延伸至 a=1 的参数盒专门保留 δ=1−a² 因子，真正的 a=1 则另外证明。

### 2.6 大次数部分不是有限采样外推

m≥10⁶ 的证明保留解析单调性。常数程序只核对起点的有理比较，不能代替这些单调性论证。正文和 Appendix A.1 分别给出推导和常数依据。

## 3. 第二轮：独立实现、等号与溯源

### 3.1 新增第二套实现

`verify_independent.py` 不导入 `certify.py` 或 `verify_certificate.py`。坐标、积分权重和系数用 `fractions.Fraction` 精确计算；幂和平方根用 160 位定向整数包围。积分权重由线性插值基函数的原函数重新计算，而不是直接调用原程序的权重公式。

它核验了与原程序相同的 6593 个参数盒，结果同样为 PASS。原核验器使用 100 位二进制定向区间。两者是同一数学规范的分别实现，均在本次 AI 辅助工作中形成，**不是两名独立数学家的审稿，更不是 Lean 形式化证明**。

### 3.2 实质加强：等号分类

原候选从 s<1 出发，只排除严格反例。本轮逐项检查后，发现可在 0<a<1 时排除 s≤1：极向指数比较的严格性来自 t²<t，而不是依赖 s<1；其余解析及证书检验仍保留严格余量。这给出所有内部零点处的严格不等式。

在 a=0 处，系数比较实际给出更强的 s≥(m+1)^(2/m)>1。在 a=1 处，新稿写出可由经典倒数恒等式直接推得的非负分解：

\[
\sum_j\frac1{|1-c_j|^2}-m
=\sum_j\frac{|c_j|^2}{|1-c_j|^2}
+2\sum_i\frac{1-|\zeta_i|^2}{|1-\zeta_i|^2}.
\]

等号迫使所有临界点位于原点，从而得到 A(z^n−ω) 的完整分类。这个非负分解作为已知恒等式的直接后果使用，不把它包装成另一个公开难题的解决。

### 3.3 重要归属与书目更正

- 边界倒数恒等式可追溯至 Meir–Sharma 1969 年第 461 页公式 (3.2)。Tang–Zhang 2025 年第 26 页公式 (5.1) 与 Remark 5.1 也使用了它。不能只引用较新的来源而遗漏原始归属。
- Phelps–Rodriguez 文章的作者是 **D. Phelps 和 R. S. Rodriguez**；刊物为 **Kodai Mathematical Seminar Reports**，24 (1972), 172–175，而不是把后来的刊名混入旧卷。其极值多项式猜想在第 174 页 Theorem 5 之后。
- Rubinstein 文章页码为 **159–161**；Bojanov–Rahman–Szynal 文章页码为 **281–285**。没有采用部分二手页面上的错误页码。
- Dégot、Chalebgwa、Tao 的纸本发表信息和作者版页码明确分开。未取得最终出版 PDF 时，正文写明“author version”，不把 arXiv 页码伪装成期刊页码。
- 无页码的 Tao 博客采用 Section、Lemma、Proposition 和公式号定位。未查到 DOI 的博客、预印本或旧书不补造 DOI。
- Miller 的另一种二次加强与本稿不是同一结论，新增 Mil25 引用予以区分。

原始巨大阈值稿所列两个无法取得的网上工作文件，没有被当作已核验的公开来源使用。本稿所需推导均已完整重写，且没有对这些未取得文件的页码作认证。具体来源等级见 `reference_audit_zh.md`。

## 4. 第三轮：逐项证明与写作检查

下表覆盖新稿全部 19 个带编号的数学环境；常规最大值公式、积分权重和附录比较另行检查。

| 新稿项目 | 内容与本轮处理 |
|---|---|
| Conjecture 1.1 | 核对 TZ25 第 5 页 Conjecture 1.10；保留 λ≥1 原始范围。 |
| Theorem 1.2 | 合并低次数、有限证书、解析尾部、端点；新增且证明全部等号情形。 |
| Corollary 1.3 | 幂平均推导；单独说明 λ≥2 的等号须回到二次等号。 |
| Lemma 2.1 | 从 P′ 因式分解直接积分及微分；归属 Mazur/Tao；无需外部结论替代。 |
| Lemma 2.2 | 均方—几何平均和删因子估计；保留平均假设，无逐点上界。 |
| Lemma 3.1 | 莫比乌斯因子模长与极向积分；明确分母非零。 |
| Lemma 3.2 | 在 s=1 时仍严格；使用充分的较弱有理估计，避免额外调用因子三引理。 |
| Lemma 3.3 | 弦积分直接计算；平方配方给 x²≥ρ，改进旧稿的 x²>ρ/4。 |
| Lemma 4.1 | 中心化插值的精确微分恒等式；验证取消一次项及合法除法。 |
| Proposition 4.2 | 从原点身份和中心化界得到两种标量必要条件；严格区分 u、s、v。 |
| Proposition 5.1 | 近边界积分分两段；端点保留 δ 尺度，避免边界极限缺口。 |
| Proposition 5.2 | 递减段/递增段及两个 a 子区间；保留 a²=1−δ 的相关性。 |
| Lemma 6.1 | 参数盒上的共同极向下界；单调性归属改为弦积分。 |
| Lemma 6.2 | 对 h 的单调性、端点和指数的取值方向逐个核查；补充辅助函数正性。 |
| Proposition 6.3 | 四种排除测试；三次最大值的分支只需对上界参数判断。 |
| Lemma 6.4 | 非负凸函数及 B^q/D 的 Hessian；精确计算两套加权弦积分。 |
| Proposition 6.5 | 两套实现实际全域回放；完整覆盖及 SHA–256 身份校验。 |
| Lemma 7.1 | 低次数四次积分因式分解；通过幂平均同时覆盖 m≤4；适当归属旧极向方法。 |
| Lemma 7.2 | 系数比较及经典边界倒数身份；增加非负分解并完成等号分类。 |

### 4.1 写作结构

引言按“几何问题的来源—边界与低次数—高次数—普通与平均障碍的区别—原猜想—主定理—核心想法—文章组织”展开。概念性说明强调方差损失由均值亏损吸收。新增一个原生 TikZ 证明路线图，不用图片替代论证。

正文没有模仿某位作者的标志性措辞；使用独立、直接的数学说明。删去非必要的高阶余项展开，并将程序实现细节放在有限验证节与附录，而不是混在概念性引言里。

### 4.2 符号与排版

统一用 ζ 表示原指定零点、a 表示其旋转后的实坐标、c_j 表示原临界点、q_j 表示倒数坐标。中心化偏差使用 ξ_j，避免与积分指数 r 混淆。JSON 坐标整数使用 L_a、U_a、H₀，避免与极向量 A₊ 重名。

所有定理环境后均无可选方括号标题。证明主定理与推论的提示放在证明前的过渡句里。删除一条没有被引用的方程编号；保留的每条编号公式均有引用。书目采用用户指定的作者—年份短标签、AMS 风格，按作者字母顺序排列，17 条全部被正文引用。摘要只列结果，关键词为五个。

用户模板中的“没有使用数据”已改成符合事实的说明：没有经验数据，但有限证书和程序是证明的必要补充材料。保留用户提供的经费声明；其真实性不是数学核查或网络查证所能替作者确认的。

### 4.3 编译与视觉检查

实际完成多次 pdflatex 编译，最终 PDF 为 17 页 Letter 纸型。检查全部页面的渲染图，并检查原生文本边界；修复了首面垂直溢出、图示重复超链接目标和书目行距警告。最终编译没有未定义引用、重复标签、重复超链接目标或 overfull/underfull 警告。自动检查结果见 `editorial_audit.json`。

## 5. 尚需由作者及独立研究者完成的工作

这份交付不包含外部审稿意见，也不包含证明助手对解析归约的形式化。两份程序通过，只保证它们所实现的有限标量证书得到接受；数学归约仍须独立逐行检查。

部分历史文献未能获取最终出版全文。正文已避免给出未经核实的印刷页定位，并在来源报告中列出具体限制。特别是 Hayman 旧书的原页、Miller 1993 年原文以及若干付费期刊的最终版本，不能声称本次已逐页核查。

投稿前还应把必要的证书和代码存入可长期访问、可版本固定的公开资料库，并将真实存档标识写入 Data availability。当前对话中的下载地址不是永久学术存档，不能用作期刊发表后的唯一复现来源。本次没有虚构 Zenodo DOI 或已经公开的代码仓库。

**最终判断：** 现有材料形成了一份包含完整解析归约、有限证书及第二套核验实现的全次数二次结果稿；本轮检查没有发现仍未交代的必要数学步骤，但这一内部判断不替代独立同行对新定理的确认。新增加的等号分类有明确证明依据，没有把 λ=1 或其他不同的公开问题并入未经证明的结论。
